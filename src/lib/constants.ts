/**
 * Vertical offset (in px) applied when scrolling to an in-page section, so the
 * target heading clears the fixed navigation bar. Shared by the nav and the
 * deep-link scroll handler to keep them in sync.
 */
export const NAV_SCROLL_OFFSET = 80;
